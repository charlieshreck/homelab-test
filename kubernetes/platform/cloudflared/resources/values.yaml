replicaCount: 2

config:
  ingress:
    # Catch-all for *.shreck.io
    - hostname: "*.shreck.io"
      service: https://traefik.traefik.svc.cluster.local:443
      originRequest:
        originServerName: shreck.io
        noTLSVerify: false
        http2Origin: true
    
    # Apex domain
    - hostname: shreck.io
      service: https://traefik.traefik.svc.cluster.local:443
      originRequest:
        originServerName: shreck.io
        noTLSVerify: false
    
    - service: http_status:404